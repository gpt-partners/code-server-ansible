---
- name: Install Caddy
  hosts: code_server
  become: true

  vars:
    code_server_user: root
    code_server_port: 8080

  tasks:
    - name: Install Caddy dependencies
      ansible.builtin.apt:
        name:
          - debian-keyring
          - debian-archive-keyring
          - apt-transport-https
          - curl
        state: present
        update_cache: true

    - name: Download Caddy GPG key
      ansible.builtin.get_url:
        url: https://dl.cloudsmith.io/public/caddy/stable/gpg.key
        dest: /tmp/caddy.gpg

    - name: Convert GPG key to keyring format
      ansible.builtin.command: >
        gpg --dearmor -o /usr/share/keyrings/caddy-stable-archive-keyring.gpg /tmp/caddy.gpg
      args:
        creates: /usr/share/keyrings/caddy-stable-archive-keyring.gpg

    - name: Download Caddy repo file
      ansible.builtin.get_url:
        url: https://dl.cloudsmith.io/public/caddy/stable/debian.deb.txt
        dest: /etc/apt/sources.list.d/caddy-stable.list
        mode: "0644"

    - name: Ensure correct permissions for keyring
      ansible.builtin.file:
        path: /usr/share/keyrings/caddy-stable-archive-keyring.gpg
        mode: "0644"

    - name: Ensure correct permissions for repo list
      ansible.builtin.file:
        path: /etc/apt/sources.list.d/caddy-stable.list
        mode: "0644"

    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true

    - name: Install Caddy
      ansible.builtin.apt:
        name: caddy
        state: present

    - name: Configure Caddy reverse proxy
      ansible.builtin.copy:
        dest: /etc/caddy/Caddyfile
        content: |
          {{ domain_name }} {
              reverse_proxy localhost:{{ code_server_port }}
          }

    - name: Restart Caddy
      ansible.builtin.systemd:
        name: caddy
        enabled: true
        state: restarted
